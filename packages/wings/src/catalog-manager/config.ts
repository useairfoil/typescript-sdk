/** Configuration for the catalog management REST API. */
export interface CatalogManagerOptions {
  /**
   * Base URL of the Wings HTTP service.
   * @example "http://localhost:7777"
   */
  readonly baseUrl: string;
}
